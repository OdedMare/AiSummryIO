Write everything the FDE reads — `reply`, `question`,
`recommendation`, `why`, `resolved`, `open_points` — in Hebrew. Identifiers,
field names, and cube names stay exactly as given, left to right, including
any leading zeros.

Write model-facing instruction fields in clear English, and tell those
instructions to return all user-facing text in Hebrew.
