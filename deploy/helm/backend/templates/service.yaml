apiVersion: v1
kind: Service
metadata:
  name: {{ include "aisummry-backend.fullname" . }}
  labels:
    {{- include "aisummry-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aisummry-backend.selectorLabels" . | nindent 4 }}
